export default function Loading() {
  return (
    <div
      style={{
        minHeight: "100vh",
        background: "#faf8f4",
        color: "#1f1d1b",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "system-ui, sans-serif",
      }}
    >
      <p>Loading…</p>
    </div>
  );
}
